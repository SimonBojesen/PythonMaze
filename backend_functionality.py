import numpy
#maze generation functions here
def maze_gen(size):
    w, h = size, size
    #matrix = [[0 for x in range(w)], [0 for y in range(h)]]
    matrix = numpy.zeros((h, w))
    return matrix

def maze_solve():
    return 123